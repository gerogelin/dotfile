[user]
    name = George Lin
    email = lyssan10@gmail.com
[color]
    branch = auto
    diff = auto
    status = auto
    showbranch = auto
    ui = true
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[alias]
    ll = log --color --graph --pretty=format:'%Cgreen(%cd) %C(yellow)%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>'
    st = status
